/** フラグ管理用 */
export type HorensoStates = {
  isStarted: boolean;
  isTarget: boolean;
  isReason: boolean;
  checkTarget: boolean;
  checkReason: boolean;
};
export type HorensoFlags = {
  deadline: boolean;
  function: boolean;
  quality: boolean;
};
